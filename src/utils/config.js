const DEFAULT_CONFIG = {
    idleTimeout: 15 * 60 * 1000,  // 5 minutes
    defaultEffectWait: 3000,     // 3 seconds
    videoSize: { width: 1920, height: 1080 },
    defaultPort: 3037
  };

  module.exports = DEFAULT_CONFIG;